import React from 'react';

// Non-iOS stub. The real implementation lives in `AppleSignInButton.ios.tsx`.
type Props = {
  disabled?: boolean;
};

export default function AppleSignInButton(_props: Props) {
  return null;
}

