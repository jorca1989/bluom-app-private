// Web stub — GPS/maps features are not available on web.
// Props are accepted but ignored; this keeps TypeScript happy when move.tsx
// passes visible/onClose to this component on the web bundle.
type Props = { visible: boolean; onClose: () => void };
export default function OutdoorActivityModal(_props: Props) {
  return null;
}
